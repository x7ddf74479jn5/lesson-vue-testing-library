// 【練習用テストケース】キーボードインタラクションのテスト
describe.todo("練習：キーボード操作でのボタン操作", () => {
  test("Tabキーでボタン間を移動できる", async () => {
    // userEvent.setup()でユーザー操作をセットアップ
    // 初期値1でコンポーネントをレンダリング（減らすボタンを有効化するため）
    // incrementButtonとdecrementButtonを取得
    // 初期状態確認（Count: 1、減らすボタンが有効）
    // 最初のボタンにフォーカスを設定
    // user.tab()を使用してタブ移動をテスト
    // 適切なボタンにフォーカスが移動していることを検証
  });

  test("Enterキーで増やすボタンを実行できる", async () => {
    // userEvent.setup()でユーザー操作をセットアップ
    // コンポーネントをレンダリング
    // 増やすボタンを取得
    // 増やすボタンにフォーカスを当てる（マウスを使わずfocus()を使用）
    // toHaveFocus()でフォーカス状態を検証
    // await user.keyboard("{Enter}")でEnterキーを押下
    // カウントが1増えることを検証
  });

  test("Spaceキーで減らすボタンを実行できる", async () => {
    // userEvent.setup()でユーザー操作をセットアップ
    // 初期値1でコンポーネントをレンダリング
    // 減らすボタンを取得
    // 初期状態確認（Count: 1になっている）
    // 減らすボタンが有効であることを確認
    // 減らすボタンにフォーカスを当てる
    // toHaveFocus()でフォーカス状態を検証
    // await user.keyboard(" ")でSpaceキーを押下
    // カウントが0になることを検証
  });

  test("キーボードとマウス操作を組み合わせる", async () => {
    // userEvent.setup()でユーザー操作をセットアップ
    // コンポーネントをレンダリング
    // incrementButtonとdecrementButtonを取得
    // キーボード操作：増やすボタンにフォーカスを当ててEnterキーで実行
    // カウントが1になることを検証、減らすボタンが有効になることを確認
    // マウス操作：減らすボタンをクリック
    // 最終的にカウントが0になることを検証
  });
});
